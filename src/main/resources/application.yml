spring:
  datasource:
    url: jdbc:mysql://localhost:3306/snip?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: msLee9164@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create   # 개발: update / 운영: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  messages:
    basename: messages,errors

